import { FilterByBoxNamePipe } from './filter-by-box-name.pipe';

describe('FilterByBoxNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBoxNamePipe();
    expect(pipe).toBeTruthy();
  });
});
